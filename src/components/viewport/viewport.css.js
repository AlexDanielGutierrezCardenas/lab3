import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    fontSize:'50px',
    color: 'red',
    height: 50,
    textAlign:'center',
    padding: '0 30px',
  },
});